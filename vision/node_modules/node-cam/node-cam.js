var events = require('events');
var app = {};

app.createCamera = function(camera_settings){
	if (camera_settings.type){
		return require('./cameras/' + camera_settings.type)(camera_settings);
	} else {
		console.log("Invalid camera settings. Please provide a valid camera type.");
	}
}

module.exports = app;